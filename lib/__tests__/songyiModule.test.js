const assert = require('assert');
const songyiModule = require('../index.js');

describe('songyiModule', () => {
  it('has a test', () => {
    assert(false, 'songyiModule should have a test');
  });
});
